# Prints a string of text to the screen
puts "I will now count my chickens:"
# Prints a string to the screen and uses interpolation to count hens: 25 plus 30 divided by 6
puts "Hens #{25.0 + 30.0 / 6.0}"
# Prints a string to the screen and uses interpolation to count roosters: 100 minus 25 multiplied by 3 modulus 4
puts "Roosters #{100.0 - 25.0 * 3.0 % 4.0}"
# Prints a string of text to the screen
puts "Now I will count the eggs:"
# Calculates 3 plus 2 plus 1 minus 5 plus 4 modulus 2 minus 1 divided by 4 plus 6
puts 3.0 + 2.0 + 1.0 - 5.0 + 4.0 % 2.0 - 1.0 / 4.0 + 6.0
# Prints a string of text to the screen
puts "Is it true that 3 + 2 < 5 - 7?"
# Calculates 3 plus and checks whether the result is less than 5 minus 7, returns a boolean value
puts 3.0 + 2.0 < 5.0 - 7.0
# Prints a string to the screen and uses interpolation to calculate 3 + 2
puts "What is 3 + 2? #{3.0 + 2.0}"
# Prints a string to the screen and uses interpolation to calculate 5 - 7
puts "What is 5 - 7? #{5.0 - 7.0}"
# Prints a string of text to the screen
puts "Oh, that's why it's false."
# Prints a string of text to the screen
puts "How about some more."
# Prints a string to the screen and uses interpolation to check whether 5 is greater than -2, returns a boolean value inside the string
puts "Is it greater? #{5.0 > -2.0}"
# Prints a string to the screen and uses interpolation to check whether 5 is greater than or equal to -2, returns a boolean value inside the string
puts "Is it greater or equal? #{5.0 >= -2.0}"
# Prints a string to the screen and uses interpolation to check whether 5 is less than or equal to -2, returns a boolean value inside the string
puts "Is it less or equal? #{5.0 <= -2.0}"
